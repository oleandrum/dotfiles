# Fonts

Place redistributable `.otf`, `.ttf` or `.ttc` files in this directory.
`./scripts/install` copies them to `~/Library/Fonts` for the current user.

Do not commit fonts whose licence does not permit redistribution.
